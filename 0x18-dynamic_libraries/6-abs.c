#include "main.h"

/**
 *_abs - a progrma that computes the absolute value of integers
 *@n: intrger tot be computed
 *Return: the absolute value of the integer
 */

int _abs(int n)

{
	if (n >= 0)

		return (n);

	else

		return (-n);

}
